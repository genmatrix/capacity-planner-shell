# Tested versions, pinned deliberately — not the newest available.
#
# Requires Python 3.11.4 or newer, and numpy is what sets that floor: 2.5.x
# publishes no cp311 wheel, so pinning it makes `pip install -r` fail on Python
# 3.11 before the app is ever reached. 2.4.6 is the newest numpy with a cp311
# build. Verified on 3.11 and on 3.14.
#
# If you bump anything here, run the full check suite against it first — the
# launcher re-syncs every environment from this file automatically.
streamlit==1.57.0
pandas==3.0.3
numpy==2.4.6
altair==6.1.0
# pyarrow is a TRANSITIVE dependency (streamlit requires it, to ship DataFrames
# to the browser) and is pinned here on purpose, not by accident: the version
# matters — newer pandas/numpy/pyarrow wheels segfaulted the demo host — and the
# rule for this file is pin what you tested. Removing the line would not remove
# the package, only the control over which build lands.
pyarrow==24.0.0
# NOT here on purpose: openpyxl. It is reachable only through data_io.py, which
# is deliberately never imported (manual entry is the model, user decision
# 2026-07-10). Verified 2026-08-04 by running the whole suite against a 3.11.4
# venv without it: all 24 green. Wiring data_io later means adding it back —
# pandas raises a clear "Missing optional dependency 'openpyxl'" if you forget.
